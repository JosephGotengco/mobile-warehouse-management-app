import {

} from "./../actions/types";

const initialState = {

}


const shiftReducer = (state = initialState, action) => {
    switch (action.type) {


        default: {
            return state;
        }
    }
}

export default shiftReducer;